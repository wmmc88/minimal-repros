[package]
name = "shared_util_lib"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tracelogging = { version = "1.2", default-features = false, features = ["etw", "macros"] }

[build-dependencies]
wdk-build = "0.5"
